# All Translate

This Python script translates the text "text" into all languages ​​supported by Google and saves the translations in a `translations.txt` file.

---

## Requirements

```bash
pip install googletrans
```

---

## Usage

```bash
python main.py
```
